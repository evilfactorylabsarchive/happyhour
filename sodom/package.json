{
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "yarn build && now dist --name sodom --target production"
  },
  "private": true,
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-wasm.rs": "^1.2.9"
  }
}
